# Ai-Diabetes-Prediction
An end-to-end machine learning project for predicting diabetes risk using patient clinical data. The project compares Logistic Regression, Random Forest, and XGBoost models, handles class imbalance, evaluates model performance using precision, recall, F1-score, and ROC-AUC, and deploys the final model through a Streamlit web application.
